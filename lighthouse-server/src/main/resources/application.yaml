lighthouse:
  sensor:
    checkRate: 30  # time in seconds between sensor state checks
    timeout: 60    # time in seconds to pass without reports before the sensor is considered unpowered
    password: changeme

logging:
  level:
    ua.org.meters.lighthouse: DEBUG
